# Frontend environment example
# Copy to .env and fill values for your environment.

# In development, point to your local backend (Vite only reads VITE_* variables)
VITE_API_URL=http://localhost:5000
